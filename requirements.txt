# Notivo Backend — Python dependencies
# All packages are already installed in this environment.
# Listed for documentation / future setup.

fastapi>=0.100.0
uvicorn>=0.20.0
httpx>=0.25.0
openai>=1.0.0
python-multipart>=0.0.5
aiosqlite>=0.19.0
pydantic>=2.0.0
